name: Notify frontend on API change

# Thin caller —— 實際邏輯在 s930415/be2fe-actions（reusable workflow）。
on:
  pull_request:
    types: [closed]
    branches:
      - main
      - uat
  workflow_dispatch:

jobs:
  notify:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event.pull_request.merged == true
    permissions:
      contents: write
    uses: s930415/be2fe-actions/.github/workflows/be-notify-frontend.yml@v1
    with:
      frontend-repo: s930415/api-fe-test
    secrets:
      dispatch-token: ${{ secrets.FE_DISPATCH_TOKEN }}
